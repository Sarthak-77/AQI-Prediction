# Base URL of the prediction API endpoint.
# Copy to `.env` for local dev, or set in your host's env vars (e.g. Vercel).
# Must point at the full /predict path.
VITE_API_URL=https://aqi-prediction-l2gz.onrender.com/predict
